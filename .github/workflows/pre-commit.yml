name: Pre-commit

on:
  pull_request:
    branches: [master]

jobs:
  run-hooks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - uses: pre-commit/action@v2.0.0s
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
